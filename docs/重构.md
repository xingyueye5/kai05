你是一位资深软件架构师。请对当前项目进行一次安全、渐进的重构，目标是让代码库更干净、精简且易于维护。

【项目概况】
- 语言/框架：{Python/TypeScript + React/...}
- 当前主要功能：{一句话描述}
- 项目根目录结构如下：

【当前痛点】
1. 多个文件存在几乎相同的工具函数（如 xxx, yyy）
2. 组件/模块职责不清，例如 {举例}
3. 存在大量注释掉的代码、未使用的 import
4. {其他你观察到的具体问题}

【重构目标】
1. 消除重复：提取公共模块/函数，统一调用
3. 文件组织：按功能/领域重新归类，每个目录的凝聚力最大化
4. 清理死代码：删除无引用的变量、函数、组件和依赖
5. 提升可读性：重命名含糊的变量/函数，保持命名风格一致

【铁律约束】
- ❗禁止改变任何现有功能行为
- ❗禁止更改公共 API / 对外接口签名
- ❗如无必要，不新增第三方依赖；如需新增，必须先向我说明理由
- ❗所有重构后，必须保证现有测试（若存在）完全通过
- ❗不能丢失任何注释中的重要业务逻辑说明

【执行流程】
1. 先扫描整个项目，列出所有可以优化的点，并分类（消除重复/拆分/移动/重命名/删除）
2. 以表格输出优化计划，包含：影响文件、操作类型、预期收益，并标明风险
3. 等待我确认后，再逐步执行修改，每次只处理一类重构，并在修改后给出变更摘要
重构需要在当前项目构建rebuild.md，并且记录修改了那些文件以及目的是什么